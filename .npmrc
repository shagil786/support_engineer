# The engines.node floor in package.json is a hard gate, not a suggestion:
# npm refuses to install on a Node older than the floor (vitest 5 requires
# >=22.12; better-sqlite3 is native). CI verifies the same floor explicitly
# via `npm run check:node` (scripts/check-node.ts).
engine-strict=true
